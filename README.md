# Installation instructions

1. Clone the repository `git clone git@github.com:nina-dk/express-graphql-with-mongoose.git`
2. Run `npm install` to install dependencies.
3. Update the `MONGO_URI` in `.env` according to your running mongodb instance.
4. Run `npm start` to start node server.
